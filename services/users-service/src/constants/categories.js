const ALLOWED_CATEGORIES = ['food', 'health', 'housing', 'sports', 'education'];

// Output order in the monthly report `costs` array.
// (The assignment sample shows: food, education, health, housing ... so we keep education second.)
const CATEGORY_REPORT_ORDER = ['food', 'education', 'health', 'housing', 'sports'];

module.exports = { ALLOWED_CATEGORIES, CATEGORY_REPORT_ORDER };
