# Tiny demo client (optional)

This is not required by the assignment — it’s just for your learning.

It calls the 4 services with Node's built-in `fetch` (Node 18+).

Run:

```bash
node client/demo.js
```
